---
title: Core Concepts
description: Understanding VoxCSS's voxel grid, camera, scene, and rendering pipeline.
---

import VoxDemo from '../../components/VoxDemo.astro';

This page covers the mental model behind VoxCSS. Each section has a live demo — drag the scenes and tweak the controls to see how things work.

## Voxel Grid

VoxCSS works on a **3D coordinate system** defined by three axes: **x** (columns), **y** (rows), and **z** (depth/layers). Every voxel occupies a cell in this grid.

Internally, the engine stacks flat CSS grid layers along the Z axis. Each layer is a 2D grid positioned with a CSS `translateZ` transform. The result is a fully 3D structure built entirely from nested HTML elements — no canvas or WebGL involved.

Coordinates are zero-indexed integers. `z: 0` is the ground layer, and higher Z values move upward.

## Camera

The camera controls how you view the scene. Drag the apple below and adjust the sliders to see each property in action.

<VoxDemo
  id="concept-camera"
  model="apple"
  controls='["zoom","pan","tilt","rotX","rotY","perspective","interactive","animate"]'
  defaults='{"zoom":0.65,"interactive":true,"showFloor":false}'
/>

- **Zoom** — scales the scene closer or further away.
- **Pan / Tilt** — shifts the viewport horizontally and vertically.
- **rotX / rotY** — rotates the view angle around the X and Y axes.
- **Perspective** — controls the 3D depth effect. Higher values feel flatter (more isometric).
- **Interactive** — enables pointer drag to rotate.
- **Animate** — auto-rotates the camera on the Y axis.

## Scene

The scene manages the 3D grid. Use the controls below to change the grid dimensions and toggle structural elements.

<VoxDemo
  id="concept-scene"
  voxels='[{"x":4,"y":4,"z":0,"color":"#f97316"}]'
  controls='["rows","cols","depth","showFloor","showWalls","projection"]'
  defaults='{"zoom":0.65,"interactive":true,"showFloor":true,"showWalls":true,"rows":8,"cols":8,"depth":6}'
/>

- **Rows / Cols / Depth** — define the grid size. Voxels outside these bounds are clipped.
- **Show Floor / Show Walls** — toggle structural planes that give the scene physical boundaries.
- **Projection** — switch between `cubic` (50px layer spacing) and `dimetric` (25px, flatter top-down feel).

## Voxel Data Model

Each voxel is a plain object. The only required fields are grid coordinates:

- **`x`, `y`, `z`** — position in the grid (required).
- **`x2`, `y2`, `z2`** — extend the voxel into a rectangular area spanning from `(x, y, z)` to `(x2, y2, z2)`. Useful for floors, walls, or any multi-cell block.
- **`color`** — any CSS color value.
- **`texture`** — apply an image or pattern to the voxel faces.
- **`shape`** — the geometry to render (see Shapes below).
- **`rot`** — rotation of the shape, snapped to 90-degree increments.

Because voxels are plain objects, you can generate them from loops, load them from JSON, or compute them from any data source.

## Shapes

VoxCSS supports several built-in shapes. The demo below shows all four — drag to inspect them from different angles.

<VoxDemo
  id="concept-shapes"
  voxels='[{"x":0,"y":0,"z":0,"color":"#f97316"},{"x":2,"y":0,"z":0,"color":"#94a3b8","shape":"ramp"},{"x":4,"y":0,"z":0,"color":"#22c55e","shape":"wedge"},{"x":6,"y":0,"z":0,"color":"#3b82f6","shape":"spike"},{"x":0,"y":2,"z":0,"color":"#94a3b8","shape":"ramp","rot":90},{"x":2,"y":2,"z":0,"color":"#94a3b8","shape":"ramp","rot":180},{"x":4,"y":2,"z":0,"color":"#22c55e","shape":"wedge","rot":90},{"x":6,"y":2,"z":0,"color":"#3b82f6","shape":"spike","rot":270}]'
  controls='["zoom","interactive"]'
  defaults='{"zoom":0.9,"interactive":true,"showFloor":true,"rotX":65,"rotY":45}'
/>

- **cube** — the default. A full cubic voxel.
- **ramp** — a sloped surface, like a roof or hillside.
- **wedge** — a triangular prism, half of a cube cut diagonally.
- **spike** — a pointed shape tapering to a tip.

All shapes snap to **90-degree rotations** via the `rot` property.

## Rendering Pipeline

VoxCSS is structured in three layers:

1. **Core** — Pure math and data. Computes merged geometries, face visibility, and grid layout. No DOM dependency — this layer can run anywhere.
2. **HTML Renderer** — Takes the core output and produces real DOM elements. It creates the CSS grid layers, positions cuboids with transforms, and applies colors and textures.
3. **Framework Wrappers** — Thin bindings for React and Vue that expose the engine as declarative components (`VoxCamera`, `VoxScene`). These wrap the HTML renderer and handle reactivity, lifecycle, and prop updates for you.

The vanilla JS `renderScene` function uses the HTML renderer directly, skipping the framework layer entirely.

## Merge Modes

When you have many adjacent voxels of the same color, VoxCSS can **merge** them into fewer, larger HTML elements. Toggle merge 3d below to see the difference in rendering.

<VoxDemo
  id="concept-merge"
  model="apple"
  controls='["zoom","mergeVoxels","showFloor"]'
  defaults='{"zoom":0.65,"interactive":true,"showFloor":false,"mergeVoxels":"3d"}'
/>

- **`false`** — No merging. Every voxel is its own element. Best for scenes where each voxel needs individual interaction.
- **`"2d"`** — Merges adjacent same-color voxels within each Z layer.
- **`"3d"`** — Volumetric merging across all three axes. Fewest elements, best performance.
