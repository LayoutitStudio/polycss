---
title: Introduction
description: Getting started with VoxCSS
---

VoxCSS is a CSS voxel engine — a 3D grid for the DOM. It renders HTML cuboids by stacking CSS grid layers and applying transforms.

## Installation

```bash
npm install @layoutit/voxcss
```
