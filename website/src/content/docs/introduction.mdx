---
title: Introduction
description: Getting started with VoxCSS — a CSS voxel engine that renders 3D grids in pure DOM/CSS.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

VoxCSS is a **CSS voxel engine** — a 3D grid for the DOM. It renders HTML cuboids by stacking CSS grid layers and applying CSS transforms. No `<canvas>`, no WebGL, no dependencies — just pure DOM and CSS.

Every voxel is a real HTML element. You can inspect it in DevTools, style it with CSS, and interact with it using standard DOM events. VoxCSS brings the simplicity of pixel art into three dimensions, right inside your web page.

## Framework Support

VoxCSS ships with first-class bindings for **React** and **Vue**, plus a vanilla JavaScript API that works anywhere. Pick whatever fits your stack.

## Installation

<Tabs syncKey="pkg">
<TabItem label="npm">
```bash
npm install @layoutit/voxcss
```
</TabItem>
<TabItem label="yarn">
```bash
yarn add @layoutit/voxcss
```
</TabItem>
<TabItem label="pnpm">
```bash
pnpm add @layoutit/voxcss
```
</TabItem>
</Tabs>

### CDN

You can also load VoxCSS directly from a CDN with no build step:

```html
<script type="module">
  import { renderScene } from "https://unpkg.com/@layoutit/voxcss";
</script>
```

## Hello World

A single orange voxel with an interactive camera — drag to rotate the scene.

<Tabs syncKey="fw">
<TabItem label="React">
```tsx
import { VoxCamera, VoxScene } from "@layoutit/voxcss/react";

export function App() {
  const voxels = [{ x: 1, y: 1, z: 0, color: "#f97316" }];
  return (
    <VoxCamera interactive>
      <VoxScene voxels={voxels} />
    </VoxCamera>
  );
}
```
</TabItem>
<TabItem label="Vue">
```vue
<template>
  <VoxCamera interactive>
    <VoxScene :voxels="voxels" />
  </VoxCamera>
</template>
<script setup lang="ts">
import { VoxCamera, VoxScene } from "@layoutit/voxcss/vue";
const voxels = [{ x: 1, y: 1, z: 0, color: "#f97316" }];
</script>
```
</TabItem>
<TabItem label="Vanilla JS">
```html
<div id="voxcss"></div>
<script type="module">
  import { renderScene } from "@layoutit/voxcss";
  renderScene({
    element: document.getElementById("voxcss"),
    camera: { interactive: true },
    scene: {
      voxels: [{ x: 1, y: 1, z: 0, color: "#f97316" }],
      showFloor: true,
    },
  });
</script>
```
</TabItem>
</Tabs>

## Next Steps

- [Core Concepts](/core-concepts) — Understand the voxel grid, camera, scene, and rendering pipeline.
